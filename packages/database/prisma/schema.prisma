model Lead {
  id        Int      @id @default(autoincrement())
  name      String
  email     String   @unique
  message   String
  source    String?  // opcional: web, chatwidget, etc.
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}